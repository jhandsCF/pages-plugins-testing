{
  "name": "pages-plugins",
  "scripts": {
    "build": "npm run build --workspaces --if-present",
    "build:example:worker": "npm run build:worker --workspace=./packages/example",
    "build:example:pages": "npm run build:worker --workspace=./packages/example",
    "postbuild": "npm run build:example:pages",
    "prestart": "npm run build",
    "start": "npm run start --workspace=./packages/example"
  },
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "@cloudflare/workers-types": "^3.3.1",
    "typescript": "^4.5.5",
    "wrangler": "file:../wrangler2/packages/wrangler"
  }
}
