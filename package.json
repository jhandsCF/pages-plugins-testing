{
  "name": "pages-plugins",
  "private": true,
  "scripts": {
    "build": "npm run build --workspaces --if-present",
    "prestart": "npm run build",
    "start": "npm run start --workspace=./packages/example"
  },
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "typescript": "^4.5.5",
    "wrangler": "https://prerelease-registry.developers.workers.dev/runs/1806889192/wrangler"
  }
}
